# Project coordinates
group=org.jvmscript
version=5.0-SNAPSHOT

# Build performance
org.gradle.caching=true
org.gradle.parallel=true
org.gradle.configuration-cache=true

# NOTE: publishing credentials (Maven Central, signing keys) belong in
# ~/.gradle/gradle.properties on the publishing machine, never in this file.
